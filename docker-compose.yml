services:
  kmapp-cms:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: kmapp-cms
    ports:
      - "1337:1337"
    environment:
      # App settings (generated by Strapi - DO NOT CHANGE)
      - APP_KEYS=kxINEqHpNplskSzi2W03Hw==,Ep7r9nrtPxakNaGxO00nkA==,A4t0bf/X8c+FkFEOwtkn1g==,H0mSfAR0zszx+cZZaOFypA==
      - API_TOKEN_SALT=Ylxl/v+XyUqnQXKyBa3tMQ==
      - ADMIN_JWT_SECRET=99VrO0ZFkH/1YLxJ58+mRA==
      - TRANSFER_TOKEN_SALT=GocqggiazfRmPo5JksipFQ==
      - JWT_SECRET=WLW4iSTiTpp2nWXReBl99A==

      # Server settings
      - HOST=0.0.0.0
      - PORT=1337

      # Database settings (connects to kmapp-postgres)
      - DATABASE_CLIENT=postgres
      - DATABASE_HOST=kmapp-postgres
      - DATABASE_PORT=5432
      - DATABASE_NAME=strapi
      - DATABASE_USERNAME=kmapp
      - DATABASE_PASSWORD=Anchienhoang2024!
      - DATABASE_SSL=false
    volumes:
      # Persist uploads (or use S3 in production)
      - ./public/uploads:/opt/app/public/uploads
    restart: unless-stopped
    networks:
      - kmapp-network

networks:
  kmapp-network:
    name: kmapp-network
    external: true

